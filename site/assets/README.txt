Move your existing logo.png into this folder.

The new mkdocs.yml references it as:
    logo: assets/logo.png
    favicon: assets/logo.png

If you'd rather keep logo.png at the docs root (as it was before),
just change those two lines in mkdocs.yml back to `logo.png`.
